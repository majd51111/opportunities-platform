# App
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Auth (wire up when implementing authentication)
# AUTH_SECRET=
# AUTH_URL=http://localhost:3000

# Database (wire up when implementing persistence)
# DATABASE_URL=
